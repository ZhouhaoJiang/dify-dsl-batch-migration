# ========== 源环境配置 / Source Environment Configuration ==========
SOURCE_DIFY_URL=https://origin.dify.ai
# 源 Dify 环境的基础 URL / Base URL of the source Dify environment

SOURCE_BEARER_TOKEN=your_source_bearer_token_here
# 用于源环境认证的 Bearer Token / Bearer token for authenticating to source

SOURCE_REFRESH_TOKEN=your_source_refresh_token_here
# （可选）自动刷新 Token 所需的 Refresh Token，留空则不自动刷新
# (Optional) Refresh token for auto-renewal; leave empty if not needed

# ========== 目标环境配置 / Target Environment Configuration ==========
TARGET_DIFY_URL=https://target.dify.ai
# 目标 Dify 环境的基础 URL / Base URL of the target Dify environment

TARGET_BEARER_TOKEN=your_target_bearer_token_here
# 用于目标环境认证的 Bearer Token / Bearer token for authenticating to target

TARGET_REFRESH_TOKEN=your_target_refresh_token_here
# （可选）自动刷新 Token 所需的 Refresh Token，留空则不自动刷新
# (Optional) Refresh token for auto-renewal; leave empty if not needed

# ========== 迁移配置 / Migration Settings ==========
APP_IDS_TO_MIGRATE=
# 要迁移的应用 ID（逗号分隔），留空则迁移全部应用
# Comma-separated list of app IDs to migrate; empty = all apps

INCLUDE_SECRET=false
# 是否迁移敏感信息（如 API Key），true 表示包含
# Whether to include sensitive data like API keys (true/false)

BACKUP_DIR=./backups
# 迁移前备份存储目录 / Directory to store backups before migration

LOG_LEVEL=INFO
# 日志级别（DEBUG, INFO, WARN, ERROR）/ Logging level

CONCURRENT_JOBS=3
# 并发迁移任务数量 / Number of concurrent migration jobs

MAX_RETRIES=3
# 每个失败任务的最大重试次数 / Max retry attempts per failed job

DRY_RUN=false
# 是否为“干运行”模式，仅测试不执行实际操作
# Dry run mode: if true, simulate only, no real changes
